const initialState = {
  query: '',
};

function searchFiltersReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default searchFiltersReducer;
